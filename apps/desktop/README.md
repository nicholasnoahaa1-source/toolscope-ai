# apps/desktop (placeholder)

Empacotamento do JARVIS Universal como aplicativo Windows. Ainda não
implementado.

A avaliação e instalação de Rust/Tauri será feita em uma etapa própria, com
plano curto e aprovação antes de instalar qualquer toolchain nova (Regra 11
de `CLAUDE.md`). Até lá, o produto roda como PWA (web) — ver `apps/web`.
