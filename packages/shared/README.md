# packages/shared (placeholder)

Tipos e contratos compartilhados entre `apps/web` e `apps/api` (ex.: formato
de mensagem de chat, estados do assistente). Vazio nesta etapa — será
preenchido quando `apps/web` e `apps/api` precisarem compartilhar tipos além
do que cada um já define localmente.
